import React from 'react';
import styled from "styled-components";

interface StandartCustomText {
    fontFamily?: string;
    fontWeight?: string;
    fontSize?: string;
    maxWidth?: string;
}


const StandartCustomText = () => {
    return (
        <div></div>
    );
};

export default StandartCustomText;